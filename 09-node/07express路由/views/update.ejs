<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<hr>
<h1>修改</h1>
<form action="/update-mons" method="post">
    <input type="hidden" name="id" value="<%= user.id %>">
    <div>
        姓名：<input type="text" name="name" value="<%= user.name %>">
    </div>
    <div>
        年龄：<input type="text" name="age" value="<%= user.age %>">
    </div>
    <div>
        性别：<input type="radio" <%= (user.sex == '男') ? 'checked' : '' %> name="sex" value="男">男
        <input type="radio" <%= (user.sex == '女') ? 'checked' : '' %> name="sex" value="女">女
        <input type="radio" <%= (user.sex == '不男不女') ? 'checked' : '' %> name="sex" value="不男不女">不男不女
        <input type="radio" <%= (user.sex == '既男又女') ? 'checked' : '' %> name="sex" value="既男又女">既男又女
    </div>
    <div>
        地址：<input type="text" name="address" value="<%= user.address %>">
    </div>
    <div>
        <input type="submit">
    </div>
</form>

</body>
</html>