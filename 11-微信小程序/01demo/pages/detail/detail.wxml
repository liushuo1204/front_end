<view>
	<image src="{{item.pic}}" mode=""/>
	<view>{{item.title}}</view>
	<view>{{item.desc}}</view>

	<button type="primary" bind:tap="back">返回上一页</button>
	<button type="warn" bind:tap="go">跳转到tabbar</button>
</view>